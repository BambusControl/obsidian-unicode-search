export interface QUserCodepointData {
    /* Turn these to Dates, not supported by json tho */
    firstUsed: number;
    lastUsed: number;
    useCount: number;
    /* Calculate Frequency of Usage */
}
