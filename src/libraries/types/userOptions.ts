import {CharacterFilterOptions} from "./characterFilterOptions";

/**
 * Plugin settings modifiable by the user.
 */
export interface UserOptions {
    /**
     * The character filter options.
     */
    characterFilter: CharacterFilterOptions;
}
